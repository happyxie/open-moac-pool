Must change the "address" in payout.json to your pool moac address.

Run the following in separate screen:
./build/bin/open-moac-pool config.json

./build/bin/open-moac-pool api.json

./build/bin/open-moac-pool unlocker.json

./build/bin/open-moac-pool payout.json